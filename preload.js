document.addEventListener('DOMContentLoaded', function () {
    const navBodyViewsElement = document.getElementById('nav-body-views');
    const childElement = navBodyViewsElement.getElementsByTagName('webview')[0];
    // childElement.setAttribute('plugins', '');
});